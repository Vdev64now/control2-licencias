# control2-licencias

Static status endpoints.

Files under `licencias/` are plain JSON documents used as a remote status
indicator. Each file contains a single object with a `status` field whose
value is either `active` or `revoked`.

File names are opaque identifiers. No personal data is stored here.
